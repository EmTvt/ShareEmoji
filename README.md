# 这是本人用于毕设实现的Flutter跨平台表情包管理App

## 运行项目: 
### 启动Flutter App
Run flutter pub get once to install dependencies, then flutter run -d <device> (e.g., flutter run -d chrome for web or flutter run -d ios for the iOS simulator).